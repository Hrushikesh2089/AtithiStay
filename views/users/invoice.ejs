<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Booking Invoice</title>

  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"
  integrity="sha512-SnH5WK+bZxgPHs44uWIX+LLJAJ9/2PkPKZ5QiAj6Ta86w+fsb2TkcmfRyVX3pBnMFcV7oQPJkl9QevSCWr3W6A=="
  crossorigin="anonymous" referrerpolicy="no-referrer" />

  <!-- CSS FOR PDF DESIGN -->
  <style>
    * {
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      margin: 0;
      padding: 20px;
      background-color: #f0f2f5;
      color: #333;
    }

    .invoice-container {
      max-width: 900px;
      margin: auto;
      background-color: #fff;
      padding: 30px 40px;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }

    .header, .footer {
      text-align: center;
      margin-bottom: 30px;
    }

    .header h2 {
      margin: 0;
      color: #ff1466;
    }

    .section {
      margin-bottom: 30px;
      border-top: 1px solid #ddd;
      padding-top: 20px;
    }

    .section h3 {
      margin-bottom: 15px;
      font-size: 20px;
      color: #2c3e50;
      border-left: 4px solid #3498db;
      padding-left: 10px;
    }

    .details-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      row-gap: 10px;
      column-gap: 40px;
    }

    .details-grid p {
      margin: 0;
    }

    .bold {
      font-weight: 600;
      color: #555;
    }

    .listing-image {
      width: 200px;
      height: 150px;
      border-radius: 8px;
      margin-bottom: 20px;
      box-shadow: 0 0 5px rgba(0,0,0,0.15);
    }

    .status {
      padding: 12px;
      font-weight: bold;
      text-align: center;
      border-radius: 6px;
      margin-top: 10px;
    }

    .listing-flex {
        display: flex;
        align-items: flex-start;
        gap: 30px;
    }

    .booking-details-split {
        display: flex;
        justify-content: space-between;
        gap: 30px;
        flex-wrap: wrap;
    }

    .column {
        flex: 1;
        min-width: 250px;
    }

    .column p {
        margin-bottom: 10px;
    }

    .details-columns {
        display: flex;
        gap: 40px;
        flex-wrap: wrap;
        margin-top: 5px;
    }

    .details-columns .column {
        flex: 1;
        min-width: 200px;
    }

    .details-columns .column p {
        margin-bottom: 10px;
    }

    .accepted {
      color: #27ae60;
    }

    .declined {
      color: #e74c3c;
    }

    .pending {
      color: #f39c12;
    }

    a {
      color: #2980b9;
      text-decoration: none;
      font-weight: 500;
    }

    .footer p {
      font-size: 14px;
      color: #888;
    }

    @media print {
      body {
        background: none;
        padding: 0;
      }

      .invoice-container {
        box-shadow: none;
        padding: 20px;
      }
    }
  </style>
</head>
<body>
  <div class="invoice-container">
    <div class="header">
      <h2><i class="fa-regular fa-compass logo-icon"></i>&nbsp;AtithiStay Booking Invoice</h2>
      <p>Invoice generated for your recent booking</p>
    </div>

    <% if(mybooking) { %>

      <!-- LISTING DETAILS SECTION -->
        <div class="section">
            <h3>Listing Details</h3>
            <h4><span class="bold"></span> <%= mybooking.listing.title %></h4>
            <div class="listing-flex">
                <img class="listing-image" src="<%= mybooking.listing.image[0].url %>" alt="Listing Image" />
                <div class="details-columns">
                    <div class="column">
                        <p><span class="bold">Destination:</span> <%= mybooking.listing.destination %></p>
                        <p><span class="bold">Address:</span> <%= mybooking.listing.address %>, <%= mybooking.listing.country %></p>
                    </div>
                    <div class="column">
                        <p><span class="bold">Owner:</span> <%= mybooking.listing.owner.username %></p>
                        <p><span class="bold">Onwer email:</span> <%= mybooking.listing.owner.email %></p>
                        <p><span class="bold">Onwer contact:</span> <%= mybooking.listing.owner.ownerContact %></p>
                    </div>
                </div>
            </div>
        </div>

        <!-- BOOKING DETAILS SECTION -->
        <div class="section">
            <h3>Booking Details</h3>
            <div class="booking-details-split">
                <div class="column">
                    <p><span class="bold">Check-In:</span> <%= new Date(mybooking.checkInDate).toLocaleDateString('en-GB') %></p>
                    <p><span class="bold">Check-Out:</span> <%= new Date(mybooking.checkOutDate).toLocaleDateString('en-GB') %></p>
                    <p><span class="bold">Guests:</span> <%= mybooking.guests.adults %> adults, <%= mybooking.guests.childrens %> childrens</p>
                    <p><span class="bold">Total Nights:</span> <%= mybooking.totalNights %></p>
                    <p><span class="bold">Price/Night:</span> ₹<%= mybooking.listing.price.toLocaleString('en-IN') %></p>
                </div>
                <div class="column">
                    <p><span class="bold">Base Price:</span> ₹<%= (mybooking.listing.price * mybooking.totalNights).toLocaleString('en-IN') %></p>
                    <p><span class="bold">Service Fee (10%):</span> ₹<%= mybooking.paymentAmount.finalServiceFee %></p>
                    <p><span class="bold">GST (18%):</span> ₹<%= mybooking.paymentAmount.finalGstAmount %></p>
                    <p><span class="bold">Total amount Paid:</span> ₹<%= mybooking.paymentAmount.finalPaymentAmount %></p>
                </div>
            </div>
        </div>

        <!-- PAYMENT SCREENSHOT -->
        <div class="section">
            <h3>Payment Screenshot</h3>
            <p><a href="<%= mybooking.paymentScreenshot[0].url %>" target="_blank">Click here to view payment screenshot</a></p>
        </div>

        <!-- STATUS SECTION -->
        <div class="section">
            <h3>Status</h3>
            <% if (mybooking.status === 0) { %>
                <div class="status accepted">Congratulations! Your booking is Accepted.</div>
            <% } else if (mybooking.status === 1) { %>
                <div class="status declined">Regrets! Your booking is Declined.</div>
            <% } else if (mybooking.status === 2 ) { %>
                <div class="status pending">Please wait! Your booking is Pending.</div>
            <% } %>
        </div>

    <% } %>

    <!-- THANK YOU SECTION -->
    <div class="footer">
      <p>Thank you for booking with AtithiStay!</p>
    </div>
  </div>
</body>
</html>
